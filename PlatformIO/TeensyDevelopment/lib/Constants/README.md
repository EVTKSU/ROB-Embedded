# Constants Files

### Overview 

The files in the `Constants` folder are values used throughout the entirety of the codebase. The most important ones are the `TransmitterConstants` and `IOConstants` which are used for values of the pins, the serial baudrates, the behavioral constants, and some other key values 