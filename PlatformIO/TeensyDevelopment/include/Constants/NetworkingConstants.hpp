#ifndef EVT_NETWORKING_CONSTANTS
#define EVT_NETWORKING_CONSTANTS

namespace Constants {
  /**
   * @brief Constants used for networking values 
   * @note This is mostly used for values like IP addresses
   */
  struct NetworkingConstants {

  };
};

#endif // EVT_NETWORKING_CONSTANTS